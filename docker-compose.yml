services:
  ai-vpn-proxy:
    build: .
    container_name: ai-vpn-proxy
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun:/dev/net/tun
    ports:
      - "127.0.0.1:8888:8888"
    volumes:
      - ./vpn:/vpn:ro
    restart: unless-stopped